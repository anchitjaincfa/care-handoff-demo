import patterns from "./claim-patterns.json"; export const BANNED_CLAIM_PATTERNS=patterns.map((pattern)=>new RegExp(pattern,"i")); export function findClaimViolations(text:string):string[]{ return BANNED_CLAIM_PATTERNS.filter((pattern)=>pattern.test(text)).map(String); }
